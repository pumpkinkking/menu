<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.menu.menu.mapper.OrderMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.menu.menu.entity.Order">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="order_no" property="orderNo"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="status" property="status"/>
        <result column="payment_time" property="paymentTime"/>
        <result column="delivery_time" property="deliveryTime"/>
        <result column="created_time" property="createdTime"/>
        <result column="updated_time" property="updatedTime"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, order_no, total_amount, status, payment_time, delivery_time, created_time, updated_time
    </sql>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from `order` where id = #{id}
    </select>

    <!-- 根据用户ID查询 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from `order` where user_id = #{userId}
    </select>

    <!-- 根据订单号查询 -->
    <select id="selectByOrderNo" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from `order` where order_no = #{orderNo}
    </select>

    <!-- 新增 -->
    <insert id="insert" parameterType="com.menu.menu.entity.Order">
        insert into `order` (user_id, order_no, total_amount, status, payment_time, delivery_time, created_time, updated_time)
        values (#{userId}, #{orderNo}, #{totalAmount}, #{status}, #{paymentTime}, #{deliveryTime}, now(), now())
    </insert>

    <!-- 更新 -->
    <update id="updateById" parameterType="com.menu.menu.entity.Order">
        update `order`
        set user_id = #{userId},
            order_no = #{orderNo},
            total_amount = #{totalAmount},
            status = #{status},
            payment_time = #{paymentTime},
            delivery_time = #{deliveryTime},
            updated_time = now()
        where id = #{id}
    </update>

    <!-- 删除 -->
    <delete id="deleteById">
        delete from `order` where id = #{id}
    </delete>
</mapper>